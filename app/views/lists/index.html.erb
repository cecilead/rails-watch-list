<div class="banner">
  <div class="container"  style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path best-new-films-coming-out-2021-2022-1631714829_opmeer, height: 300, crop: :fill %>')>
    <h1>Lists</h1>
    <p>Create your own List</p>
  <%= link_to  new_list_path, class:'ms-3' do%>
    <div class="btn btn-primary">Add a new list</div>
  <% end %>
  </div>
</div>


<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
    </li>
  <% end %>
</ul>
